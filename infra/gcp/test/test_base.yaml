apiVersion: batch/v1
kind: Job
metadata:
  name: test-base-svm
spec:
  template:
    spec:
      containers:
      - name: test-base-svm
        image: kundajelab/lsgkm
        imagePullPolicy: Always
        resources:
          requests:
            memory: 10Gi
            cpu: 1
          limits:
            memory: 16Gi
            cpu: 3
        command:
        - /bin/bash
        - -c
        args:
        - echo "Beginning test-base";
          gsutil cp gs://gbsc-gcp-lab-kundaje-user-amtseng/test/test_base_svm.sh ~;
          bash ~/test_base_svm.sh;
      restartPolicy: Never
  backoffLimit: 0
